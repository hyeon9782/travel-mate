import styled from "styled-components";

const Cities = () => {
  return (
    <CitiesBlock>
      <div></div>
      <div></div>
    </CitiesBlock>
  );
};

const CitiesBlock = styled.section``;

export default Cities;
